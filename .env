# Copia questo file in ".env" (senza ".example") e personalizza i
# valori — .env NON va mai committato su Git (è già in .gitignore),
# contiene credenziali reali.

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=ats_webapp_db
POSTGRES_USER=ats_webapp
POSTGRES_PASSWORD=1234
